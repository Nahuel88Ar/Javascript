
function perimetrodeuncuadrado(N){
    let perimetro = 4*N
    return perimetro;
}


/*
function calcularPerimetroCuadrado(longitudLado) {
    // Calcular el perímetro del cuadrado (lado * 4)
    let perimetro = longitudLado * 4;
    return perimetro;
}

// Ejemplo de uso de la función
let longitudLado = prompt("Ingresa la longitud de un lado del cuadrado:"); // Longitud de un lado del cuadrado
let resultado = calcularPerimetroCuadrado(longitudLado);
console.log(`El perímetro del cuadrado con lado ${longitudLado} es: ${resultado}`);
*/
